{
  "name": "react-express-redux-poc",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "client": "cd frontend && npm start",
    "dev": "cross-env NODE_ENV=development concurrently \"npm run server\" \"npm run client\"",
    "server": "node server.js",
    "install:deps": "npm install && cd frontend && npm install",
    "start": "cross-env NODE_ENV=production && cd frontend && npm run build && cd .. && npm run server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.1.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.2",
    "express": "^4.17.1"
  }
}
